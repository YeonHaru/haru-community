@page
@model DeletePersonalDataModel
@{
    ViewData["Title"] = "개인 정보 삭제";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h3 class="mb-3">@ViewData["Title"]</h3>

<div class="alert alert-warning" role="alert">
    <p class="mb-0">
        <strong>계정을 삭제하면 모든 데이터가 영구적으로 제거되며 복구할 수 없습니다.</strong>
    </p>
</div>

<div>
    <form id="delete-user" method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
        @if (Model.RequirePassword)
        {
            <div class="mb-3">
                <label asp-for="Input.Password" class="form-label"></label>
                <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true" placeholder="현재 비밀번호를 입력하세요." />
                <span asp-validation-for="Input.Password" class="text-danger small"></span>
            </div>
        }
        <button class="btn btn-danger w-100" type="submit">모든 데이터 삭제 및 계정 탈퇴</button>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
