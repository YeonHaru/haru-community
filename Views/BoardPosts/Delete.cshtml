@model BoardPost

<h1 class="mb-3">게시글 삭제</h1>

<div class="alert alert-warning">
    <strong>주의:</strong> 삭제하면 되돌릴 수 없습니다. 이 게시글을 삭제할까요?
    <div class="mt-2"><span class="badge bg-light text-dark">@Model.Board?.Name</span></div>
</div>

<dl class="row">
    <dt class="col-sm-2">제목</dt>
    <dd class="col-sm-10">@Model.Title</dd>

    <dt class="col-sm-2">작성자</dt>
    <dd class="col-sm-10">@Model.AuthorName</dd>

    <dt class="col-sm-2">작성일</dt>
    <dd class="col-sm-10">@Model.CreatedAt.ToLocalTime().ToString("yyyy-MM-dd HH:mm")</dd>
</dl>

<form asp-action="Delete" method="post">
    <input type="hidden" asp-for="Id" />
    <input type="hidden" asp-for="BoardId" />
    <div class="d-flex justify-content-between">
        <a asp-action="Index" asp-route-boardId="@Model.BoardId" class="btn btn-link">취소</a>
        <button type="submit" class="btn btn-danger">삭제</button>
    </div>
</form>
